name: ci
on: 
 push:
   branches:
     - dev
jobs:
  testing:
    runs-on:
      - ubuntu-latest 
    steps:
      - name: checkout repo
        uses: actions/checkout@v6.0.2

      - name: testing....
        run: echo "php artisan test --parallel"
