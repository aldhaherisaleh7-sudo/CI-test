name: "daily tasks"

on:
  schedule:
    - cron: "* * * * *" # every minute

jobs:
  daily-task:
    runs-on: ubuntu-latest

    steps:
      - name: "Print message"
        run: echo "Running daily tasks..."
